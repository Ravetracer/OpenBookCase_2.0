security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'
    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        # used to reload user from session & other features (e.g. switch_user)
        # No `property:` so UserRepository::loadUserByIdentifier() is used, which
        # accepts either the username or the e-mail address as the login identifier.
        app_user_provider:
            entity:
                class: App\Entity\User
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        # OAuth2 token endpoint — the bundle authenticates the client itself; no firewall identity.
        oauth2_token:
            pattern: ^/oauth/token$
            security: false
        # Public API (Phase 3 endpoints). Bearer-token authenticated; lazy + stateless so
        # requests without a token pass through anonymously (public-data reads stay open),
        # while writes are gated per-route by an OAuth scope (ROLE_OAUTH2_*).
        api:
            pattern: ^/api/v1
            lazy: true
            stateless: true
            security: true
            oauth2: true
        main:
            lazy: true
            provider: app_user_provider
            user_checker: App\Security\UserChecker
            custom_authenticator: App\Security\AppAuthenticator
            remember_me:
                secret: '%kernel.secret%'
                lifetime: 2592000 # 30 days, in seconds
                # opt-in: only set the cookie when the login form's
                # "_remember_me" checkbox is checked (handled by the
                # RememberMeBadge added in AppAuthenticator)
                always_remember_me: false
            logout:
                path: app_logout
                # where to redirect after logout
                # target: app_any_route

            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#the-firewall

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        - { path: ^/admin, roles: ROLE_ADMIN }
        # The consent screen needs a logged-in user; anonymous → redirected to login first.
        - { path: ^/oauth/authorize, roles: ROLE_USER }
        # - { path: ^/profile, roles: ROLE_USER }

when@test:
    security:
        password_hashers:
            # By default, password hashers are resource intensive and take time. This is
            # important to generate secure password hashes. In tests however, secure hashes
            # are not important, waste resources and increase test times. The following
            # reduces the work factor to the lowest possible values.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
